# Minifetch

a small fetch tool I made to practice C a little

# COMPILATION INSTRUCTIONS

clang: `clang ASCII.c main.c -o minifetch`  
gcc: `gcc ASCII.c main.c -o minifetch`  

if you don't know what C compiler you use, substitute gcc/clang for cc